# Server
server.port=${PORT:8080}

# MongoDB — env var wins if set, otherwise falls back to Atlas URI
spring.data.mongodb.uri=${SPRING_DATA_MONGODB_URI:mongodb+srv://rafey:rafeynoori@cluster0.oo5ezm1.mongodb.net/sonify?retryWrites=true&w=majority&appName=Cluster0}

# CORS — allowed origin(s) per environment
sonify.cors.allowed-origins=${ALLOWED_ORIGINS:http://localhost:4200,http://localhost:52384}

# File upload limits
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
